version: 1
id: WO-0065
epic_id: E-0020
title: '[WO-PENDING-NORMALIZATION-P1] Normalize active pending hybrids'
priority: P1
status: pending
owner: null
branch: null
worktree: null
scope:
  allow:
  - _ctx/jobs/pending/*.yaml
  - _ctx/jobs/grandfathered_pending_ids.yaml
  - docs/plans/*.md
  - _ctx/handoff/**/handoff.md
  - tests/unit/test_wo_*
  - tests/integration/test_wo_*
  deny:
  - _ctx/jobs/done/*.yaml
  - _ctx/jobs/failed/*.yaml
  - docs/guides/**
verify:
  commands:
  - uv run python scripts/ctx_backlog_validate.py --strict
  - uv run pytest -q tests/unit/test_wo_* tests/integration/test_wo_* -k "normalize or grandfather or
    pending"
dod_id: DOD-DEFAULT
execution:
  engine: trifecta
  required_flow:
  - session.append:intent
  - ctx.sync
  - ctx.search
  - ctx.get
  - verify
  - session.append:result
  segment: .
dependencies:
- WO-0063
- WO-0064
trace:
  ticket: null
  related_wos:
  - WO-0063
  - WO-0064
  - WO-0066
objective: Normalize only the 7 tracked hybrid WOs in pending, extract long narrative out of active YAML,
  and drain the grandfathering manifest.
tracked_pending_ids:
- WO-0016
- WO-0017
- WO-0018C
- WO-0020-formatter
- WO-0021-verdict-generator
- WO-0037
- WO-0039
deliverables:
- Canonical pending WOs for the tracked active hybrid set
- Narrative extracted to docs/plans/ or _ctx/handoff/
- Manifest entries removed as each WO becomes canonical
acceptance_criteria:
- The tracked 7 hybrids (WO-0016, WO-0017, WO-0018C, WO-0020-formatter, WO-0021-verdict-generator, WO-0037,
  WO-0039) become canonical_current.
- pending/ ends the wave 100 percent canonical.
- The grandfathering manifest is empty or only retains explicitly deferred tracked IDs with a documented
  reason.
out_of_scope:
- done/failed historical cleanup
- Broad corpus normalization outside the tracked active set
artifacts:
- _ctx/jobs/pending/*.yaml
- _ctx/jobs/grandfathered_pending_ids.yaml
- docs/plans/*.md
- _ctx/handoff/**/handoff.md
risks:
- Narrative extraction loses operator context
- Active work gets blocked if a normalized WO loses take-ready fields
definition_of_done:
- All 7 frozen IDs are normalized and validated.
- Manifest removals happen in the same changes that normalize the WOs.
- Any deferred ID is named explicitly with a reason; no implicit carry-over is allowed.
- Verify commands pass.
rollback_note: Revert individual normalizations if needed; never re-open old hybrid intake.
x_alias: WO-PENDING-NORMALIZATION-P1
x_wave: 3
x_gate: Drains the manifest and ends transition mode
