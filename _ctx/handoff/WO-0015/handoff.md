# Handoff: WO-0015

## Summary
Implement a topology scanner that recursively maps a directory tree,
identifying Python modules and packages. Uses WO-0014's ImportExtractor
to build an import graph (DAG) of the codebase.


## Evidence
